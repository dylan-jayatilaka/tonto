module DATAFILE

   implicit none

contains

   open(filename)
   ! Open the file
      self :: INOUT
      filename :: STR
      self.sbf = sbf_File(filename)
   end

   close
   ! Close the file
      self :: IN
      call self.sbf.close
   end

end
